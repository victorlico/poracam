# config.txt — Poracam v0.6
# Formato: chave=valor
# Linhas começando com # são comentários.

run_mode=single

# Duração de cada gravação, em segundos.
record_duration_s=60

# Período total do ciclo, em segundos. Ainda não executa loop na v0.6.
cycle_period_s=300

# Para uso em pendrive/SSD, deixe media_dir comentado/ausente.
# Assim, se este arquivo estiver em PORACAM/config.txt, o Poracam salva em PORACAM/media.
# media_dir=/home/fishcam/poracam/media

max_storage_percent=95
prefer_external_storage=true
allow_local_fallback=true

width=1280
height=720
fps=30
bitrate=2500000

audio_device=default
audio_format=cd
keep_h264=false

# Política contra câmera ocupada por RPiCam/raspimjpeg, motion etc.
# Opções: error_only, stop_known_processes, ignore
camera_conflict_policy=stop_known_processes

# Tempo para checar se o raspivid falha logo no início.
# Se falhar nesse período, o áudio não é iniciado.
camera_startup_check_s=1.0

# Tempo para tentar parar processos conhecidos da câmera.
camera_stop_timeout_s=5.0

# Na v0.6 o vídeo inicia primeiro. Este atraso é aplicado antes de iniciar o áudio,
# depois que o vídeo passou na checagem inicial.
start_delay_s=0.2

extra_timeout_s=15